binsearch(a[], n, x)
{
  while (low =< high)do {
    mid = (low + high)/2
    if (x<a[mid]) {
      /* code */high = mid - 1;
    } else if (/* condition */x>a[mid]) {
      /* code */low = mid + 1;
    }
    else return mid;
  }
  return 0;
}
